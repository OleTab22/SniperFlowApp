<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent" android:layout_height="wrap_content"
    android:layout_marginVertical="6dp" android:layout_marginHorizontal="12dp">

    <LinearLayout android:orientation="vertical" android:padding="12dp">
        <LinearLayout android:orientation="horizontal">
            <TextView android:id="@+id/tvTitle" android:textStyle="bold"
                android:text="XAUUSD · M5" android:layout_width="0dp"
                android:layout_weight="1" android:layout_height="wrap_content"/>
            <TextView android:id="@+id/tvRr" android:text="R:R —"
                android:background="@android:color/darker_gray"
                android:textColor="@android:color/white"
                android:padding="6dp"/>
        </LinearLayout>

        <LinearLayout android:orientation="horizontal" android:layout_marginTop="6dp">
            <TextView android:id="@+id/chipBias" android:text="Bull"
                android:background="@android:color/transparent" android:padding="4dp"/>
            <TextView android:id="@+id/chipSession" android:text="London"
                android:layout_marginStart="8dp" android:padding="4dp"/>
            <TextView android:id="@+id/chipQueued" android:text="Queued to sync"
                android:layout_marginStart="8dp" android:padding="4dp"
                android:background="@android:color/holo_orange_dark"
                android:textColor="@android:color/white" android:visibility="gone"/>
        </LinearLayout>

        <TextView android:id="@+id/tvSubtitle"
            android:text="MSS sweep + DO @ 2 431.20"
            android:layout_marginTop="6dp"/>

        <LinearLayout android:id="@+id/tagsRow" android:orientation="horizontal"
            android:layout_marginTop="8dp"/>
    </LinearLayout>
</com.google.android.material.card.MaterialCardView>


